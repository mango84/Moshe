pipeline {
    agent { node {label 'master'}  }
    stages {
        stage('Run Script') {
            steps {
                powershell '''$R = $ENV:BUILD_NUMBER / 3
                              if ($R.GetType().Name -eq "Double") {
                              throw "Script Failed"
                              } else {
                                "Script Succeeded"
                              }
                           '''
            }
        }
    }
}