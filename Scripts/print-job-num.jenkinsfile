pipeline {
    agent { label 'master' }
    parameters {
        string(name: 'JOB_NUM', defaultValue: '', description: '')
    }
    
    stages {
        stage('Print Job Num') {
            steps {
                script {
                    echo env.JOB_NUM
                }
            }
        }
    }
}