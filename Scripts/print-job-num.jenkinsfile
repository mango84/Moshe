pipeline {
    agent { label 'master' }
    parameters {
        string(name: 'JOB_NUM', defaultValue: '', description: '')
    }
    
    stages {
        stage('Print Job Num') {
            steps {
                script {
                    echo Job Number of job1 is env.JOB_NUM
                }
            }
        }
    }
}